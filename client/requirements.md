## Packages
otpauth | Zero-dependency TOTP generation for browser
react-markdown | For rendering markdown documents
remark-gfm | GitHub flavored markdown support for tables and tasklists
date-fns | Date formatting

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
}

The backend must return accurate valid base32 secrets for OTP auth to function properly.
Images/Icons: Using Lucide React for crisp, clean minimalist iconography matching the aesthetic.
